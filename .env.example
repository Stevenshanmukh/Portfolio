# Supabase Configuration (Required)
# Get these from: Supabase Dashboard → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key

# Revalidation Secret (Optional but recommended)
# Used for instant site updates after admin saves
# Can be any random string - just make sure both match
REVALIDATION_SECRET=your-random-secret-string
NEXT_PUBLIC_REVALIDATION_SECRET=your-random-secret-string
